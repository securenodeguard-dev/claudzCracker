{
  "name": "cracker-shop",
  "version": "1.0.0",
  "private": true,
  "description": "Fireworks/cracker shop monorepo: consumer-portal, admin-portal (Angular) + backend (NestJS)",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix consumer-portal && npm install --prefix admin-portal",
    "dev:backend": "npm run start:dev --prefix backend",
    "dev:consumer": "npm start --prefix consumer-portal",
    "dev:admin": "npm start --prefix admin-portal",
    "seed:admin": "npm run seed:admin --prefix backend",
    "test:backend": "npm test --prefix backend"
  }
}
